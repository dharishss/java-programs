import java.util.Vector;

public class Smallestletter {
	
	   public static char nextGreatestLetter(Vector<Character> letters, char target) 
	    {

		int start=0;
		int end= arr.length-1;
		
		while(start <=end)
			
		{
			int mid = start + (end-start)/2;
		if(target> arr[mid])
		{
			start = mid+1;
		}
		else if ( target <arr[mid]) {
			end = mid-1;
		}
		
	}
		return start;
	}

	public static void main(String[] args) {
          int[] arr = {'c','f','j'};
          int target = 'f';
          int ans = nex(arr, target);
          System.out.println(ans);
	}

}
